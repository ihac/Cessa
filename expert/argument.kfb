## Application type
# Web, Database
relevant(Web, (AF_LOCAL, AF_INET, AF_INET6))

## Detail
# IPv4, IPv6
relevant(IPv4, (AF_INET, AF_LOCAL))
relevant(IPv6, (AF_INET6, AF_LOCAL))
relevant(Novell, (AF_IPX))
conflicting(IPv4, (AF_INET6))
conflicting(IPv6, (AF_INET))

relevant(TCP, (SOCK_STREAM))
relevant(UDP, (SOCK_DGRAM))
